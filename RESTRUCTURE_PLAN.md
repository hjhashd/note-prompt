# 提示词系统 UI/UX 重构计划 (版本 3.1 - 平铺导航版)

## 1. 核心设计变革
为了最大化内容区的展示空间，并提供更直观的权限层级感知，我们将移除内容区左侧的分类树面板，转而采用**“内容区顶部多级平铺筛选栏”**。

*   **保持**: 主侧边栏 (Sidebar.vue) 结构不变，保留现有导航项。
*   **移除**: 内容区左侧的独立树状目录面板 (PromptCategorySidebar)。
*   **引入**: 位于内容区顶部的“级联平铺标签栏”，通过横向平铺方式展示 3 级部门/分类结构。
*   **空间**: 提示词列表（Prompt List）扩展至全宽，提升阅读体验。

---

## 2. UI 布局结构

### 2.1 主侧边栏 (Main Sidebar) - **保持现状**
根据现有 UI 截图，侧边栏保留以下核心入口：
*   **公共文件夹** (当前选中)
*   **我的提示词**
*   **我的收藏**
*   **个人中心**
*   **管理员面板**
*   **底部用户信息** (admin)

### 2.2 级联平铺筛选栏 (Cascade Tiled Filter Bar)
位于内容区顶部（面包屑下方），根据后端 3 级树结构动态渲染：
*   **Level 1 (一级部门)**: 选项如 `[ 全部公司 ] [ 客户中心 ] [ 研发中心 ] ...`
*   **Level 2 (二级部门/项目组)**: 选项如 `[ 全部 ] [ 项目一部 ] [ 项目二部 ] ...`
*   **Level 3 (三级自定义分类)**: 用户自定义分类，如 `[ 全部 ] [ 方案库 ] [ 话术库 ] ...`

### 2.3 视图头部 (View Header)
*   **面包屑**: 保持路径指引，如 `首页 / 客户中心 / 项目一部`。
*   **搜索与操作**: 标题右侧保留全局搜索框及“+ 新建”按钮。

---

## 3. 业务逻辑与权限对应

### 3.1 分享机制支撑
平铺标签的层级与系统的分享权限完美契合：
*   **分享到全部**: 在 Level 1 选中“全部公司”时显示。
*   **分享到部门**: 在 Level 1 选中具体部门，且 Level 2 为“全部”时显示。
*   **分享到项目组**: 在 Level 2 选中具体项目组时显示。

### 3.2 默认聚焦逻辑
*   账号绑定到二级部门（如：客户中心-项目一部）。
*   用户登录后，系统自动初始化 Level 1 为“客户中心”，Level 2 为“项目一部”，让用户第一时间看到所属组织的提示词。

---

## 4. 技术实现要点

### 4.1 级联组件设计
*   创建一个通用组件 `TiledCategoryFilter.vue`。
*   **状态管理**: 维护 `activeIds: [level1Id, level2Id, level3Id]` 数组。
*   **计算属性**: 根据父级 ID 过滤出当前行应显示的子级列表。

### 4.2 布局调整
*   在 `MyPromptsView.vue` 或 `PublicFolder.vue` 中，将原有的 `PromptCategorySidebar` 替换为顶部的 `TiledCategoryFilter`。
*   设置内容区容器为 `flex-direction: column`，确保筛选栏位于列表上方。

---

## 5. 预期改动文件
*   `src/views/MyPromptsView.vue`: 核心布局调整，引入新筛选组件。
*   `src/views/PublicFolder.vue`: 同步布局调整。
*   `src/components/prompt/TiledCategoryFilter.vue`: (新建) 核心平铺筛选组件。
*   `src/components/prompt/PromptCategorySidebar.vue`: (废弃) 移除左侧树状面板。
*   *注：`src/components/layout/Sidebar.vue` 保持不变。*

version: '3.8'

services:
  note-prompt:
    extends:
      file: docker-compose.yml
      service: note-prompt
    environment:
      - VITE_APP_ENV=staging
      # 使用 Docker 内部网络别名，不要写外部 IP
      - PYTHON_API_URL=http://python-backend:8000
      - JAVA_API_URL=http://java-backend:8080
